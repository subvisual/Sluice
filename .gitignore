# Dependencies
node_modules/

# Secrets — never commit a key, this repo signs transactions
.env
.env.*
!.env.example

# Hardhat
artifacts/
cache/
typechain-types/
# ignition/deployments/ is deliberately NOT ignored — the whole team needs our
# self-deployed Aqua addresses, and they are the hour-0 gate.

# Foundry
out/
broadcast/
lib/forge-std/
contracts/lib/
contracts/out/
contracts/cache/

# The Graph
subgraph/build/
subgraph/generated/
subgraph/tests/.bin/
subgraph/tests/.latest.json
# local fork stack artifacts (anvil.log is covered by *.log)
subgraph/local/.anvil.pid
subgraph/local/networks-local.json
subgraph/subgraph.local.yaml

# Build output
dist/
build/
coverage/
*.tsbuildinfo

# Logs
*.log
npm-debug.log*
yarn-error.log*

# Editors / OS
.DS_Store
.idea/
.vscode/
!.vscode/extensions.json

# Local agent scratch — decision traces, fixtures, throwaway scripts
.scratch/
docs/superpowers/
.vercel
.env*
