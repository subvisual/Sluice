{
  "name": "sluice",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace @sluice/app",
    "build": "npm run build --workspace @sluice/app",
    "typecheck": "npm run typecheck --workspace @sluice/app",
    "lint": "npm run lint --workspace @sluice/app"
  },
  "overrides": {
    "@wagmi/core": "2.22.1"
  }
}
