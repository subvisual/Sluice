# PRDs

`<feature-slug>.md` and `<feature-slug>-issues.md`, one pair per feature, using the slugs in
`docs/features/`.

Conventions and how the building skills use this directory: `docs/agents/prds.md`.
