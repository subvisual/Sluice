# The venue. Defaults to a local anvil fork of Base at the pinned block.
# Point it at Base mainnet only for step 2 — and note that chainId reads 8453
# either way, so this string is the only thing that tells them apart here.
NEXT_PUBLIC_RPC_URL=http://127.0.0.1:8545
